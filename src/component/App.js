import React, {Component} from 'react'
import {BrowserRouter, Route, Link} from 'react-router-dom'

const App = () =>  (
    <BrowserRouter>
        <div>
            <Route exact pass='/login' component={}/>
            <Route exact pass='/login' component={}/>
        </div>
    </BrowserRouter>
)
